<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CuteKana" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/cream</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/cream</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Splash Screen Theme - Using AndroidX SplashScreen API -->
    <style name="Theme.CuteKana.Splash" parent="Theme.SplashScreen">
        <!-- Use the app theme as the parent for the final theme -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenBackground">@color/sakura_pink</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/sakura_pink</item>
        <item name="postSplashScreenTheme">@style/Theme.CuteKana</item>

        <!-- Status and navigation bar colors -->
        <item name="android:statusBarColor">@color/sakura_pink</item>
        <item name="android:navigationBarColor">@color/sakura_pink</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>